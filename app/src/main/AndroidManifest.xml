<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wengel.astenagaj">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name=".util.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".main_views.SplashScreenActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".main_views.WelcomeActivity"
            android:label="@string/title_activity_welcome" />
        <activity
            android:name=".main_views.ManagerActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".main_views.CustomerActivity"
            android:label="@string/title_activity_customer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".customer.AddOrderActivity"
            android:label="@string/title_addneworder_acctivity" />
        <activity android:name=".customer.BillActivity" />
        <activity
            android:name=".manager.ManagerLoginActivity"
            android:label="@string/title_activity_manager_login" />
        <activity android:name=".manager.OrderDetailActivity" />
        <activity android:name=".manager.PaymentStatusActivity" />
        <activity android:name=".manager.EmployeeDetailActivity" />
        <activity android:name=".manager.EmployeeEvaluationActivity" />
        <activity android:name=".manager.AddNewEmployeeActivity" />
        <activity android:name=".manager.SalesReportActivity" />
        <activity android:name=".customer.menus.TableAndQuantityActivity"
            android:label="@string/title_activity_table_and_quantity"></activity>
    </application>

</manifest>